version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_APP_NAME: "AegisStream Studio"
        VITE_SUPABASE_URL: "http://127.0.0.1:54321"
        VITE_SUPABASE_ANON_KEY: ""
    ports:
      - "8080:8080"
    restart: unless-stopped
